app:
  serverPort: 8080
  logLevel: "debug"

mailer:
  provider:
    - name: "amazon"
      type: "amazon-ses"
      enabled: true
      priority: 1
      idKey: "-"
      apiKey: "-"
      sender:
        name: sender
        email: sendmailtest@sharkslasers.com

    - name: "sendgrid"
      type: "sendgrid"
      enabled: true
      priority: 2
      idKey: "-"
      apiKey: "-"
      sender:
        name: sender
        email: sendmailtest@sharkslasers.com
